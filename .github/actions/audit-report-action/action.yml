name: 'Audit Report'
description: 'Run audit on projects and report vulnerabilities'
inputs:
    projects:
      required: true
    github-token:
      required: true
    github-context:
      required: false
      default: ${{ toJson(github) }}
runs:
  using: 'node20'
  main: 'dist/index.js'